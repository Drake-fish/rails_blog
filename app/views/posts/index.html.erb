<h1>Here is all the Posts</h1>

<p> <%= link_to "Create a new Post", new_post_path %> </p>

<ul class="post-list">
  <% @posts.each do |post| %>
  <li class="post-preview-container">
    <h5><%= post.title %></h5>
    <p><%= post.body %></p>
    <p> <%= link_to "Edit", edit_post_path(post) %> | <%= link_to "Delete", post_path(post), method: :delete, data: {confirm: "Are you sure?"} %> </p>
  </li>
  <% end %>
</ul>
