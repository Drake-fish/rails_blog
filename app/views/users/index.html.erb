<h1>All of the users</h1>

<% @users.each do |user| %>
  <%= gravatar_for user %>
  <%= link_to user.username, user_path(user) %>
  <p><%= pluralize(user.posts.count, "post") if user.posts %></p>
<% end %>
